export interface dessert {
    name: string,
    calories: number,
    fat: number, 
    carbs: number,
    protein: number
}

export interface titles {
    title: string,
    key: string,
    sortable?: Boolean
}